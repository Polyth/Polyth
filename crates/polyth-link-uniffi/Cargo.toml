[package]
name = "polyth-link-uniffi"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Stable native ABI over the shared Polyth Link client core"

[lib]
name = "polyth_link_uniffi"
crate-type = ["cdylib", "staticlib", "rlib"]

[dependencies]
polyth-link-core = { path = "../polyth-link-core" }
polyth-link-client = { path = "../polyth-link-client" }
serde_json.workspace = true
tokio.workspace = true
zeroize.workspace = true

[dev-dependencies]
tempfile = "3.20.0"
