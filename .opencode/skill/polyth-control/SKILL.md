---
name: polyth-control
description: Legacy entry point for explicitly authorized control of a running Polyth instance, not general repository development.
---
# Legacy Polyth control entry

Read root `AGENTS.md` and `.agents/skills/polyth-live-control/SKILL.md` before using a live instance. For code development use `docs/agents/README.md` instead.

Discover the actual available MCP tools and input schemas first. A local configuration is not evidence of a running or authorized connection. Preserve explicit targets/model choices, never send secrets, never broaden permissions, and resolve ambiguity before destructive actions. Do not repeat uncertain mutations or use paid sessions as connectivity tests. This file remains at its old path for compatibility; the maintained skill body is the canonical live-control skill.
