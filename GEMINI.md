@AGENTS.md

# Gemini CLI entry point

Use the shared repository contract. Choose a domain from `docs/agents/README.md` and read its canonical skill under `.agents/skills/`. If imports or skill discovery are disabled by the client, explicitly read root `AGENTS.md` first. Do not assume every path reference was automatically loaded.
