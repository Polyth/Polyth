// Compatibility face: the Markdown implementation lives in markdown/ (WP4).
// Chat, multirun, fusion and file preview all share this sanitized renderer.
export { renderMarkdown, MarkdownDoc } from "./markdown/render.tsx";
