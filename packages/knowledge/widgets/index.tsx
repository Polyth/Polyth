import "./styles.css";
import { createElement } from "react";
import { defineWebPackage } from "@polyth/web-sdk";
import KnowledgePanel from "./KnowledgePanel.tsx";
import TracksPanel from "./TracksPanel.tsx";
export default defineWebPackage((host) => () => { const presentation = { kind: "workspace" as const, defaultRatio: 0.6, minWidth: 320, preferredMaxWidth: 760, keepAlive: true as const, escape: "close" as const }; const off = [host.surfaces.register({ id: "knowledge", title: "Knowledge", description: "Browse notes and references Polyth can use.", capabilityId: "knowledge", order: 40, component: KnowledgePanel, presentation }), host.surfaces.register({ id: "tracks", title: "Tracks", description: "Execute a saved feature specification.", capabilityId: "tracks", order: 41, component: () => createElement(TracksPanel), presentation }), host.capabilities.register({ id: "knowledge", label: "Knowledge", plainDescription: "Notes and references Polyth can use.", keywords: ["knowledge", "notes", "docs"], standardTier: "more", standardRank: 17, open: () => host.navigation.openWorkspacePane("knowledge"), available: () => true }), host.capabilities.register({ id: "tracks", label: "Tracks", technicalLabel: "Spec-driven tracks", plainDescription: "Execute a saved feature specification.", keywords: ["track", "spec", "plan", "workflow"], standardTier: "more", standardRank: 18, open: () => host.navigation.openWorkspacePane("tracks"), available: () => true })]; return () => off.toReversed().forEach((dispose) => dispose()); });
