import "./styles.css";
import { createElement } from "react";
import { defineWebPackage } from "@polyth/web-sdk";
import KnowledgePanel from "./KnowledgePanel.tsx";
import TracksPanel from "./TracksPanel.tsx";
export default defineWebPackage((host) => () => { const off = [host.surfaces.register({ id: "knowledge", title: "Knowledge", capabilityId: "knowledge", order: 40, component: KnowledgePanel }), host.slots.register({ slot: "workspace.right.tabs", id: "tracks", order: 1, render: () => createElement(TracksPanel), meta: { title: "Tracks", capabilityId: "tracks" } }), host.capabilities.register({ id: "knowledge", label: "Knowledge", plainDescription: "Notes and references Polyth can use.", keywords: ["knowledge", "notes", "docs"], standardTier: "more", standardRank: 17, open: () => host.navigation.openRailSurface("knowledge"), available: () => true }), host.capabilities.register({ id: "tracks", label: "Tracks", technicalLabel: "Spec-driven tracks", plainDescription: "Execute a saved feature specification.", keywords: ["track", "spec", "plan", "workflow"], standardTier: "more", standardRank: 18, open: () => host.navigation.openRailSurface("slot:tracks"), available: () => true })]; return () => off.toReversed().forEach((dispose) => dispose()); });
