import "./styles.css";
import { createElement } from "react";
import { defineWebPackage } from "@polyth/web-sdk";
import WalkthroughView from "./WalkthroughView.tsx";
export default defineWebPackage((host) => () => { const off = [host.surfaces.register({ id: "walkthrough", title: "Guided changes walkthrough", description: "Review each file edit in order; approve or flag as you go.", capabilityId: "walkthrough", order: 24, component: () => createElement(WalkthroughView), presentation: { kind: "workspace", defaultRatio: 0.6, minWidth: 380, preferredMaxWidth: 760, keepAlive: true, escape: "close" } }), host.capabilities.register({ id: "walkthrough", label: "Guided walkthrough", plainDescription: "Review changed files in guided stages.", keywords: ["walkthrough"], standardTier: "more", standardRank: 13, open: () => host.navigation.openWorkspacePane("walkthrough"), available: () => true }), host.widgets.registerPlugin({ id: "walkthrough", name: "Walkthrough", widgets: [{ id: "walkthrough.main", title: "Walkthrough", description: "Review changed files in guided stages.", defaultSlot: "workspace.main", supportedSlots: ["workspace.main", "workspace.bottom"], defaultSize: { w: 12, h: 6 }, render: () => createElement(WalkthroughView) }] })]; return () => off.toReversed().forEach((dispose) => dispose()); });
